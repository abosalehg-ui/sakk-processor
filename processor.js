/**
 * معالج الصكوك القضائية — منطق المعالجة النقي (بدون أي تعامل مع DOM)
 *
 * يُستخدم هذا الملف من:
 *   - index.html في المتصفح (يُعرَّف ككائن عام SakkProcessor)
 *   - tests/run-tests.mjs في Node لتشغيل الاختبارات الذهبية
 *
 * أي تعديل على القاموس أو أنماط الدمج يجب أن يرافقه تشغيل الاختبارات:
 *   node tests/run-tests.mjs
 */
(function (root, factory) {
    if (typeof module !== 'undefined' && module.exports) {
        module.exports = factory();
    } else {
        root.SakkProcessor = factory();
    }
})(typeof self !== 'undefined' ? self : this, function () {
    'use strict';

    // علامات داخلية مؤقتة لتتبع مواضع الدمج والحذف،
    // تُحوَّل إلى تظليل في العرض وتُحذف من النتيجة النهائية
    const MERGE_START = '\u0001';
    const MERGE_END = '\u0002';
    const DELETE_MARK = '\u0003';
    const MERGED_PHRASE = MERGE_START + 'وفي جلسة أخرى' + MERGE_END;

    // نسختان من النمط: واحدة عامة (g) للحذف بـ replace،
    // وأخرى بدون g للفحص بـ test (تجنباً لحالة lastIndex المشتركة)
    const INTERNAL_MARKS_G = /[\u0001-\u0003]/g;
    const HAS_INTERNAL_MARK = /[\u0001-\u0003]/;

    // عتبة تقريبية لآلية الدمج الاحتياطية: مقطع أقصر من هذا العدد من الكلمات
    // بين «رفعت الجلسة» و«حضر» يُرجَّح أنه فاصل بين جلستين لا محتوى فعلياً
    const MIN_SESSION_WORDS = 17;

    function escapeRegex(str) {
        return str.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
    }

    // تهريب رموز HTML حتى لا تنكسر الواجهة إذا احتوى النص على وسوم أو رموز خاصة
    function escapeHtml(str) {
        return str
            .replace(/&/g, '&amp;')
            .replace(/</g, '&lt;')
            .replace(/>/g, '&gt;')
            .replace(/"/g, '&quot;')
            .replace(/'/g, '&#39;');
    }

    const corrections = [
        ["ادَّعت", "أدعت"],
        ["ادَّعى", "أدعى"],
        ["ادَّعي", "أدعي"],
        ["ادَّعيا", "أدعيا"],
        ["اعترف", "أعترف"],
        ["اكتساب", "أكتساب"],
        ["آلاف", "ألاف"],
        ["امتنع", "أمتنع"],
        ["اتِّفاق", "إتفاق"],
        ["اثنان", "إثنان"],
        ["إبَّان", "ابان"],
        ["أبو", "ابو"],
        ["أتمكَّن", "اتمكن"],
        ["إثبات", "اثبات"],
        ["أثناء", "اثناء"],
        ["إجراء", "اجراء"],
        ["أُجِيْبَ", "اجيب"],
        ["أُجِيْبَت", "اجيبت"],
        ["إحالة", "احالة"],
        ["أُحِيْلَ", "احيل"],
        ["أخرجه", "اخرجه"],
        ["إخلاء", "اخلاء"],
        ["إدارة", "ادارة"],
        ["إدخال", "ادخال"],
        ["إذا", "اذا"],
        ["إزالة", "ازالة"],
        ["أسباب", "اسباب"],
        ["أُسبوع", "اسبوع"],
        ["إشكال", "اشكال"],
        ["أصالةً", "اصالة"],
        ["أصحاب", "اصحاب"],
        ["إطلاقًا", "اطلاقا"],
        ["إعادة", "اعادة"],
        ["إعطاء", "اعطاء"],
        ["أُعْطِيَ", "اعطي"],
        ["إعلام", "اعلام"],
        ["إعلان", "اعلان"],
        ["أُعلم", "اعلم"],
        ["أُعِيدَ", "اعيد"],
        ["إفهام", "افهام"],
        ["إقامة", "اقامة"],
        ["أقرَّ", "اقر"],
        ["إقرار", "اقرار"],
        ["إكمال", "اكمال"],
        ["الإبل", "الأبل"],
        ["الاثنين", "الأثنين"],
        ["الإعلام", "الأعلام"],
        ["الاتَّصالات", "الإتصالات"],
        ["الاتِّفاق", "الإتفاق"],
        ["الاتِّهام", "الإتهام"],
        ["الاثنين", "الإثنين"],
        ["الادِّعاء", "الإدعاء"],
        ["الاستئناف", "الإستئناف"],
        ["الاستثمار", "الإستثمار"],
        ["الاستحكام", "الإستحكام"],
        ["الاستدلال", "الإستدلال"],
        ["الاستعداد", "الإستعداد"],
        ["الاستلام", "الإستلام"],
        ["الاستناد", "الإستناد"],
        ["الاشتراك", "الإشتراك"],
        ["الاطِّلاع", "الإطلاع"],
        ["الاعتراض", "الإعتراض"],
        ["الأغنام", "الإغنام"],
        ["الاقتناع", "الإقتناع"],
        ["الانتِّقال", "الإنتقال"],
        ["الانتهاء", "الإنتهاء"],
        ["الانقياد", "الإنقياد"],
        ["إلَّا", "الا"],
        ["الإبل", "الابل"],
        ["الإجراء", "الاجراء"],
        ["الإجمال", "الاجمال"],
        ["الإجماليَّة", "الاجمالية"],
        ["الإخلاء", "الاخلاء"],
        ["الأدلَّة", "الادلة"],
        ["الإسلام", "الاسلام"],
        ["الإعلام", "الاعلام"],
        ["الأغنام", "الاغنام"],
        ["الإقامة", "الاقامة"],
        ["الأقلِّ", "الاقل"],
        ["الإمام", "الامام"],
        ["الإنصاف", "الانصاف"],
        ["الأولاد", "الاولاد"],
        ["الإيداع", "الايداع"],
        ["التَّعزيز", "التعزير"],
        ["محكمة الاستئناف", "محكمة التمييز"],
        ["لمحكمة الاستئناف", "لمحكمة التمييز"],
        ["بمحكمة الاستئناف", "بمحكمة التمييز"],
        ["ومحكمة الاستئناف", "ومحكمة التمييز"],
        ["الضَّبط", "الظبط"],
        ["المتعدِّدة", "المتعدده"],
        ["المتوفَّـى", "المتوفى"],
        ["الـمُخدِّرات", "المخدرات"],
        ["الـمُخدِّرات", "المخدِّرات"],
        ["الـمُخنَّث", "المخنث"],
        ["ــ المذكور ــ", "المذكور"],
        ["ــ المذكوران ــ", "المذكوران"],
        ["ــ المذكورة ــ", "المذكورة"],
        ["ــ المذكور ــ", "المذكورر"],
        ["ــ المذكورين ــ", "المذكورين"],
        ["ــ المرفق صورته ــ", "المرفق صورته"],
        ["الـمُروِّج", "المروج"],
        ["الـمُروِّجان", "المروجان"],
        ["الـمُروِّجون", "المروجون"],
        ["الـمُروِّجين", "المروجين"],
        ["ــ المشار إليها ــ", "المشار إليها"],
        ["الـمُكلَّف", "المكلف"],
        ["المنـزل", "المنزل"],
        ["ــ المنوَّه عنها ــ", "المنوه عنها"],
        ["ــ المنوَّه عنه ــ", "المنوَّه عنه"],
        ["الـمُورِّث", "المورث"],
        ["ــ الموصوفة في الدَّعوى ــ", "الموصوفة في الدعوى"],
        ["الـمُيسَّر", "الميسر"],
        ["التَّنفيذيّ", "النفيذي"],
        ["إلى", "الى"],
        ["إليَّ", "الي"],
        ["إلينا", "الينا"],
        ["إليه", "اليه"],
        ["إليها", "اليها"],
        ["أمَّا", "اما"],
        ["إمارة", "امارة"],
        ["أنَّ", "ان"],
        ["إنشاءات", "انشاءات"],
        ["إنَّما", "انما"],
        ["أنَّنا", "اننا"],
        ["أنَّني", "انني"],
        ["أنَّه", "انه"],
        ["أنَّها", "انها"],
        ["إنهاء", "انهاء"],
        ["إنهائيَّة", "انهائية"],
        ["أنَّهما", "انهما"],
        ["أُنهِيَ", "انهي"],
        ["أهليَّة", "اهلية"],
        ["أهليَّته", "اهليته"],
        ["أولًا", "اولا"],
        ["أي", "اي"],
        ["إيَّاه", "اياه"],
        ["إيَّاها", "اياها"],
        ["إيداع", "ايداع"],
        ["إيصال", "ايصال"],
        ["أيضًا", "ايضا"],
        ["إيضاح", "ايضاح"],
        ["إيقافه", "ايقافه"],
        ["بإعلان", "بأعلان"],
        ["باتِّفاق", "بإتفاق"],
        ["ــ بإذن الله ــ", "بإذن الله"],
        ["بانتهاء", "بإنتهاء"],
        ["بإحضار", "باحضار"],
        ["بإحضاره", "باحضاره"],
        ["بإحضارها", "باحضارها"],
        ["بإذنِهِ", "باذنه"],
        ["بإعادة", "باعادة"],
        ["بإعلان", "باعلان"],
        ["بإكمال", "باكمال"],
        ["بالاحتيال", "بالإحتيال"],
        ["بالاشتراك", "بالإشتراك"],
        ["بالاطِّلاع", "بالإطلاع"],
        ["بالاعتراض", "بالإعتراض"],
        ["بالتَّـرجمة", "بالترجمة"],
        ["بالـمُخدِّرات", "بالمخدرات"],
        ["بالـمُخدِّرات", "بالمخدِّرات"],
        ["بأنَّ", "بان"],
        ["بأنَّهم", "بانهم"],
        ["بأنَّهما", "بانهما"],
        ["براءة", "برأة"],
        ["بقولها:", "بقولها"],
        ["ــ تعالى ــ", "تعالى"],
        ["تعهَّد", "تعهد"],
        ["حظيرة", "حضيرة"],
        ["حذف", "خذف"],
        ["ــ دون رقمٍ ــ", "دون رقم"],
        ["ــ رحمه الله ــ", "رحمه الله"],
        ["إذا", "غذا"],
        ["إلغاء", "غلغاء"],
        ["على", "غلى"],
        ["عن", "غن"],
        ["فأُجِيْبَت", "فاجيبت"],
        ["فأُعِيدَ", "فاعيد"],
        ["فالاحتيال", "فالإحتيال"],
        ["فالاشتراك", "فالإشتراك"],
        ["فالاعتراض", "فالإعتراض"],
        ["فالـمُخدِّرات", "فالمخدرات"],
        ["فإنَّه", "فانه"],
        ["فوق", "فقو"],
        ["فلم يبقَ", "فلم يبقى"],
        ["فقط", "قفط"],
        ["كالاختصاص", "كالإختصاص"],
        ["طباعة", "كباعة"],
        ["لابن", "لإبن"],
        ["لانتهاء", "لإنتهاء"],
        ["لأنَّني", "لانني"],
        ["لأنَّه", "لانه"],
        ["لأنَّها", "لانها"],
        ["الدُّسوقيّ", "لدسوقي"],
        ["للاحتيال", "للإحتيال"],
        ["للاستناد", "للإستناد"],
        ["للاطِّلاع", "للإطلاع"],
        ["للمنـزل", "للمنزل"],
        ["لم يبقَ", "لم يبقى"],
        ["لـما", "لما"],
        ["لـمُهرِّبي", "لمهربي"],
        ["ما لديَّ", "ما لدي"],
        ["ما لديَّ", "مالدي"],
        ["متعدِّدة", "متعدده"],
        ["مع", "مغ"],
        ["منـزلًا", "منزلا"],
        ["مؤسَّسة", "موسسة"],
        ["يـُمثِّله", "يمثله"],
        ["يـُمثِّلها", "يمثلها"],
        ["يـُمثِّلهم", "يمثلهم"],
        ["ما جاء", "ماجاء"],
        ["ما يؤيده", "مايؤيده"],
        ["أولاً", "اولاً"],
        ["استلمت", "إستلمت"],
        ["استنادًا", "إستناداً"],
        ["الإقرار", "الاقرار"],
        ["الأقوى", "الاقوى"],
        ["يـُمثِّلهما", "يمثلهما"]
    ];

    corrections.sort((a, b) => b[1].length - a[1].length);

    // تُبنى أنماط التصحيح مرة واحدة عند التحميل بدل إعادة تصريفها في كل معالجة (أداء)
    const compiledCorrections = corrections.map(function (entry) {
        const correct = entry[0];
        const wrong = entry[1];
        return {
            correct: correct,
            wrong: wrong,
            wrongRegex: new RegExp('(^|[^\\u0600-\\u06FF])' + escapeRegex(wrong) + '(?![\\u0600-\\u06FF])', 'g'),
            correctRegex: new RegExp('(^|[^\\u0600-\\u06FF])' + escapeRegex(correct) + '(?![\\u0600-\\u06FF])', 'g')
        };
    });

    const sessionMergePatterns = [
        // ========== أنماط محددة جداً (تأتي أولاً للأولوية) ==========

        // 1- وكان ختامها الساعة XX:XX صباحاً/مساءً. افتتحت الجلسة
        {
            pattern: /وكان\s+ختامها\s+الساعة\s+\d{1,2}:\d{2}\s+(?:صباحاً|صباحًا|مساءً|مساءًا)\s*\.\s*افتتحت\s+الجلسة/g,
            replacement: 'وفي جلسة أخرى'
        },
        // 2- وكان ختامها الساعة XX:XX صباحاً/مساءً. وفي هذه الجلسة
        {
            pattern: /وكان\s+ختامها\s+الساعة\s+\d{1,2}:\d{2}\s+(?:صباحاً|صباحًا|مساءً|مساءًا)\s*\.\s*وفي\s+هذه\s+الجلسة/g,
            replacement: 'وفي جلسة أخرى'
        },
        // 3- رفع الجلسة للنطق بالحكم. افتتحت الجلسة وفيها (يحافظ على المقدمة)
        {
            pattern: /رفع\s+الجلسة\s+للنطق\s+بالحكم\s*\.\s*افتتحت\s+الجلسة\s+وفيها/g,
            replacement: 'رفع الجلسة للنطق بالحكم. وفي جلسة أخرى'
        },

        // ========== أنماط متخصصة ==========

        // 4- وإذا لم يحضر يحكم عليه بالنكول. وفي هذه الجلسة
        {
            pattern: /وإذا\s+لم\s+يحضر\s+يحكم\s+عليه\s+بالنكول\s*\.\s*وفي\s+هذه\s+الجلسة/g,
            replacement: 'وإذا لم يحضر يحكم عليه بالنكول. وفي جلسة أخرى'
        },
        // 5- للدراسة والاطلاع وفي هذه الجلسة وعبر الاتصال المرئي
        {
            pattern: /للدراسة\s+والاطلاع\s+وفي\s+هذه\s+الجلسة\s+وعبر\s+الاتصال\s+المرئي/g,
            replacement: 'للدراسة والاطلاع وفي جلسة أخرى'
        },
        // 6- وبالله التوفيق. افتتحت الجلسة (مع/بدون مسافة قبل النقطة)
        {
            pattern: /وبالله\s+التوفيق\s*\.\s*افتتحت\s+الجلسة/g,
            replacement: 'وفي جلسة أخرى'
        },
        // 7- وبالله التوفيق. وفي هذه الجلسة
        {
            pattern: /وبالله\s+التوفيق\s*\.\s*وفي\s+هذه\s+الجلسة/g,
            replacement: 'وفي جلسة أخرى'
        },
        // 8أ- هذا بالله التوفيق. وعقدت هذه الجلسة
        {
            pattern: /هذا\s+بالله\s+التوفيق\s*\.\s*وعقدت\s+هذه\s+الجلسة/g,
            replacement: 'وفي جلسة أخرى'
        },
        // 8ب- هذا وبالله التوفيق. وعقدت هذه الجلسة
        {
            pattern: /هذا\s+وبالله\s+التوفيق\s*\.\s*وعقدت\s+هذه\s+الجلسة/g,
            replacement: 'وفي جلسة أخرى'
        },
        // 8ج- هذا وبالله التوفيق. عقدت الدائرة الجلسة عن بعد بقرار المجلس الأعلى للقضاء
        {
            pattern: /هذا\s+وبالله\s+التوفيق\s*\.?\s*وفي\s+هذا\s+اليوم\s+عقدت\s+الدائرة\s+هذه\s+الجلسة\s+عن\s+بعد\s+بناء\s+على\s+قرار\s+المجلس\s+الأعلى\s+للقضاء\s+رقم\s*\([^)]*\)\s*بتاريخ\s*[\d\s/]+\s*هـ\s*والمبلغ\s+بتعميم\s+معالي\s+رئيس\s+المجلس\s+الأعلى\s+للقضاء\s*[–—-]\s*وفقه\s+الله\s*[–—-]\s*برقم\s*\([^)]*\)\s*بتاريخ\s*[\d\s/]+\s*هـ/g,
            replacement: 'وفي جلسة أخرى'
        },

        // ========== أنماط رفع الجلسة ==========

        // 9- رفعت الجلسة. افتتحت الجلسة وفيها
        {
            pattern: /رفعت?\s+الجلسة\s*\.\s*افتتحت\s+الجلسة\s+وفيها/g,
            replacement: 'وفي جلسة أخرى'
        },
        // 10- رفعت الجلسة افتتحت الجلسة وفيها (بدون نقطة)
        {
            pattern: /رفعت?\s+الجلسة\s+افتتحت\s+الجلسة\s+وفيها/g,
            replacement: 'وفي جلسة أخرى'
        },
        // 11- رفع الجلسة. افتتحت الجلسة (بدون "وفيها" في الآخر)
        {
            pattern: /رفعت?\s+الجلسة\s*\.\s*افتتحت\s+الجلسة(?!\s+وفيها)/g,
            replacement: 'وفي جلسة أخرى'
        },
        // 12- رفع الجلسة في هذه الجلسة
        {
            pattern: /رفع\s+الجلسة\s+في\s+هذه\s+الجلسة/g,
            replacement: 'وفي جلسة أخرى'
        },
        // 13- رفع الجلسة وفي هذه الجلسة
        {
            pattern: /رفع\s+الجلسة\s+وفي\s+هذه\s+الجلسة/g,
            replacement: 'وفي جلسة أخرى'
        },
        // 14- رفع الجلسة. افتتحت هذه الجلسة عبر الاتصال المرئي
        {
            pattern: /رفعت?\s+الجلسة\s*\.\s*افتتحت\s+هذه\s+الجلسة\s+عبر\s+الاتصال\s+المرئي/g,
            replacement: 'وفي جلسة أخرى'
        },

        // ========== أنماط عامة (تأتي آخراً) ==========

        // 14- صباحاً/مساءً. وفي هذه الجلسة (نمط مختصر)
        {
            pattern: /(?:صباحاً|صباحًا|مساءً|مساءًا)\s*\.\s*وفي\s+هذه\s+الجلسة/g,
            replacement: 'وفي جلسة أخرى'
        }
    ];

    // أنماط بداية الجلسة — تُستخدم لتحديد حدود جلسات الشطب قبل الحذف
    const SESSION_START_SOURCES = [
        'افتتحت\\s+الجلسة',
        'فلدي\\s+أنا',
        'لدي\\s+أنا',
        'وفي\\s+هذا\\s+اليوم\\s+عقدت\\s+الد(?:َّ)?ائرة\\s+هذه\\s+الجلسة'
    ];

    const SHATB_PATTERN_SOURCE = 'لذا\\s+(?:فقد\\s+)?قررت\\s+الد(?:َّ)?ائرة\\s+شطب\\s+الدعوى';
    const SHATB_END_PATTERN = /وصحبه\s+(?:أجمعين|وسلَّم|وسلم)\s*\.?/;

    /**
     * يعالج نص الضبط ويعيد:
     *   marked:         النص المعالج وبداخله علامات داخلية لمواضع الدمج والحذف (للتظليل)
     *   result:         النص النهائي النظيف (بدون علامات) — هذا ما يُنسخ ويُحفظ
     *   correctedWords: الكلمات التي صُحِّحت (مع أنماطها المصرَّفة لاستخدامها في التظليل)
     *   stats:          إحصائيات العمليات
     */
    function processText(inputText, options) {
        options = options || {};
        const enableCorrection = options.enableCorrection !== false;

        // تعقيم رموز التحكم من المدخل حتى لا يُزوَّر التظليل الداخلي
        let text = inputText.replace(INTERNAL_MARKS_G, '');

        const stats = {
            spacesCount: 0,
            colonsCount: 0,
            sessionsCount: 0,
            deletedCount: 0,
            emptyLinesCount: 0,
            correctionsCount: 0,
            shatbSkipped: 0
        };

        // 1. استبدال المسافات المتكررة بمسافة واحدة
        stats.spacesCount = (text.match(/  +/g) || []).length;
        text = text.replace(/  +/g, ' ');

        // 2. استبدال " :" بـ ":"
        stats.colonsCount = (text.match(/ :/g) || []).length;
        text = text.replace(/ :/g, ':');

        // 3. حذف الأسطر الفارغة
        stats.emptyLinesCount = (text.match(/\n\s*\n/g) || []).length;
        text = text.replace(/\n\s*\n/g, '\n');

        // 4. دمج الجلسات — الأنماط المحددة
        sessionMergePatterns.forEach(function (entry) {
            const matches = text.match(entry.pattern);
            if (matches) {
                stats.sessionsCount += matches.length;
                text = text.replace(entry.pattern, entry.replacement.replace('وفي جلسة أخرى', MERGED_PHRASE));
            }
        });

        // 5. التصحيح اللغوي (إذا كان مفعّلاً)
        const correctedWords = [];
        if (enableCorrection) {
            compiledCorrections.forEach(function (c) {
                const foundMatches = text.match(c.wrongRegex);
                if (foundMatches) {
                    stats.correctionsCount += foundMatches.length;
                    correctedWords.push({
                        wrong: c.wrong,
                        correct: c.correct,
                        count: foundMatches.length,
                        wrongRegex: c.wrongRegex,
                        correctRegex: c.correctRegex
                    });
                    text = text.replace(c.wrongRegex, '$1' + c.correct);
                }
            });
        }

        // 4ب. آلية الدمج الاحتياطية (العامة):
        // تلتقط فواصل جلسات لم تغطها الأنماط المحددة في الخطوة 4. حدودها الموثقة:
        //   - تعمل فقط على مقاطع أقصر من MIN_SESSION_WORDS كلمة بين «رفعت الجلسة» و«حضر»
        //     وتحوي إحدى عبارات الختام المعروفة (وبالله التوفيق / ختامها / للدراسة)
        //   - تتجاهل أي مقطع سبق أن عولج بالآلية الأولى (يحوي علامات داخلية)
        //     منعاً للتداخل والعد المزدوج في الإحصائيات
        const sessionRegex = /(رفعت?\s+الجلسة)([\s\S]*?)(حضرت?(?:\s|$))/g;
        const sessionMatches = [];
        let match;
        while ((match = sessionRegex.exec(text)) !== null) {
            sessionMatches.push({
                fullMatch: match[0],
                start: match[1],
                middle: match[2],
                end: match[3],
                index: match.index
            });
        }

        for (let i = sessionMatches.length - 1; i >= 0; i--) {
            const m = sessionMatches[i];
            if (HAS_INTERNAL_MARK.test(m.middle)) {
                continue;
            }
            const wordCount = m.middle.trim().split(/\s+/).length;
            if (wordCount < MIN_SESSION_WORDS) {
                const hasClosingPhrase =
                    m.middle.includes('وبالله التوفيق') ||
                    m.middle.includes('ختامها') ||
                    m.middle.includes('للدراسة');
                if (hasClosingPhrase) {
                    const replacement = m.start + ' ' + MERGED_PHRASE + ' ' + m.end;
                    text = text.substring(0, m.index) + replacement + text.substring(m.index + m.fullMatch.length);
                    stats.sessionsCount++;
                }
            }
        }

        // إزالة ازدواج الواو الناتج عن الدمج (مثل: «ورفعت الجلسة...» → «ووفي جلسة أخرى»)
        text = text.replace(new RegExp('و' + MERGE_START + 'وفي جلسة أخرى', 'g'), MERGE_START + 'وفي جلسة أخرى');
        text = text.replace(/ووفي\s+جلسة\s+أخرى/g, 'وفي جلسة أخرى');

        // 6. حذف جلسات الشطب
        // يُقيَّد البحث عن صيغة الختام بنطاق الجلسة نفسها (حتى بداية الجلسة التالية إن وُجدت)
        // حتى لا يلتهم الحذف جلسات صحيحة إذا لم تُختم جلسة الشطب بالصيغة المتوقعة.
        // عند تعذر تحديد النهاية بأمان يُتجاوز الحذف ويُحصى في stats.shatbSkipped
        const shatbPattern = new RegExp(SHATB_PATTERN_SOURCE, 'g');
        const shatbPositions = [];
        let shatbMatch;
        while ((shatbMatch = shatbPattern.exec(text)) !== null) {
            shatbPositions.push(shatbMatch.index);
        }

        for (let i = shatbPositions.length - 1; i >= 0; i--) {
            const shatbPos = shatbPositions[i];
            const textBeforeShatb = text.substring(0, shatbPos);

            // آخر بداية جلسة قبل عبارة الشطب = بداية نطاق الحذف
            let lastStartPos = -1;
            for (const source of SESSION_START_SOURCES) {
                const re = new RegExp(source, 'g');
                let startMatch;
                while ((startMatch = re.exec(textBeforeShatb)) !== null) {
                    if (startMatch.index > lastStartPos) {
                        lastStartPos = startMatch.index;
                    }
                }
            }
            if (lastStartPos === -1) {
                continue;
            }

            const textAfterShatb = text.substring(shatbPos);

            // أول بداية جلسة تالية بعد عبارة الشطب = الحد الأقصى الآمن لنطاق الحذف
            let nextStartPos = textAfterShatb.length;
            for (const source of SESSION_START_SOURCES) {
                const re = new RegExp(source, 'g');
                const nextMatch = re.exec(textAfterShatb);
                if (nextMatch && nextMatch.index < nextStartPos) {
                    nextStartPos = nextMatch.index;
                }
            }

            const endMatch = SHATB_END_PATTERN.exec(textAfterShatb.substring(0, nextStartPos));
            if (endMatch) {
                const endPos = shatbPos + endMatch.index + endMatch[0].length;
                text = text.substring(0, lastStartPos) + DELETE_MARK + text.substring(endPos);
                stats.deletedCount++;
            } else {
                stats.shatbSkipped++;
            }
        }

        return {
            marked: text,
            result: text.replace(INTERNAL_MARKS_G, ''),
            correctedWords: correctedWords,
            stats: stats
        };
    }

    return {
        processText: processText,
        escapeHtml: escapeHtml,
        escapeRegex: escapeRegex,
        corrections: corrections,
        sessionMergePatterns: sessionMergePatterns,
        MERGE_START: MERGE_START,
        MERGE_END: MERGE_END,
        DELETE_MARK: DELETE_MARK,
        MERGED_PHRASE: MERGED_PHRASE,
        MIN_SESSION_WORDS: MIN_SESSION_WORDS
    };
});
