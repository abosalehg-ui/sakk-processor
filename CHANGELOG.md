# سجل التغييرات

## نسخة 5.4 — 2026-07-26

تنفيذ خطة الإصلاح الشاملة الناتجة عن مراجعة الكود الكاملة (هندسة + UX + أمن).

### إصلاحات حرجة (صحة النص القضائي)
- **القاموس:** إصلاح `باكمال ← بإكمال` (كانت تُسقط حرف الباء وتكسر الجملة)
- **القاموس:** إصلاح `تعهد ← تعهَّد` (كانت تحوّل المفرد إلى مثنى `تعهَّدا` وتغيّر المعنى)
- **القاموس:** إصلاح `ما لدي ← ما لديَّ` (كانت تحقن نقطة زائدة في منتصف الجملة)
- **القاموس:** حذف استبدال `ما لدى` و`يدعي` و`معزى` (كلمات وتراكيب صحيحة كانت تُستبدل خطأً)
- **حذف جلسات الشطب:** تقييد البحث عن صيغة الختام بنطاق الجلسة نفسها (حتى بداية الجلسة التالية) —
  كان الحذف قد يلتهم جلسات صحيحة كاملة إذا لم تُختم جلسة الشطب بالصيغة المتوقعة.
  عند تعذر تحديد النهاية بأمان يُتجاوز الحذف ويُنبَّه المستخدم
- **دمج الجلسات:** منع التداخل والعد المزدوج بين الأنماط المحددة والآلية الاحتياطية العامة

### بنية واختبارات
- فصل محرك المعالجة (القاموس، الأنماط، منطق الشطب) إلى `processor.js` نقي بدون DOM
- إضافة اختبارات ذهبية (`tests/run-tests.mjs`) تغطي التنظيف والتصحيح والدمج والشطب — تعمل بـ Node بدون اعتماديات
- بناء أنماط التصحيح مرة واحدة عند التحميل بدل إعادة تصريفها في كل معالجة (أداء)
- إزالة التكرار: دالة موحدة لتهريب الريقاكس وعدّ الأحرف، وثابت مسمّى لعتبة الدمج الاحتياطي
- تسجيل تفاصيل الأخطاء في Console مع عرض نوع الخطأ في رسالة المستخدم

### الخصوصية والأمن
- الحفظ التلقائي للمسودة أصبح **اختيارياً (معطلاً افتراضياً)** لحماية خصوصية النصوص القضائية
  على الأجهزة المشتركة، مع حذف تلقائي للمسودة بعد 24 ساعة وتنظيف أي مسودة قديمة متبقية
- إضافة سياسة أمن المحتوى (CSP) كوسم meta — لا يُحمَّل أي محتوى خارجي إطلاقاً
- تهريب علامة الاقتباس المفردة في `escapeHtml` (دفاع في العمق)

### تجربة المستخدم والوصولية
- حوار تأكيد قبل «محو النص» (كان يمسح النص والنتائج والمسودة بلا تراجع)
- حد أقصى لارتفاع صندوقي المقارنة مع تمرير داخلي **متزامن** بينهما لمراجعة الضبوط الطويلة
- `role="status"` و`aria-live` للتوست ومؤشر التحميل (دعم قارئات الشاشة)
- تغميق لون قيم الإحصائيات لتحقيق تباين WCAG AA
- إضافة بدائل خطوط عربية (`Noto Naskh Arabic`, `Amiri`, `Sakkal Majalla`) للأجهزة غير الويندوز
- خصائص CSS منطقية للـ RTL (`margin-inline-start`, `inset-inline-end`) ونقل التوست لجهة نهاية السطر
- إصلاح تراكب مؤقّت رسائل التوست (رسالة جديدة لم تعد تُخفى مبكراً بمؤقّت رسالة سابقة)

### توثيق
- تحديث README: بنية المشروع، طريقة تشغيل الاختبارات، الأعداد الفعلية للأنماط والقاموس
- إضافة هذا الملف (CHANGELOG.md)
